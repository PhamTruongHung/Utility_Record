<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.inqbarna.tablefixheaders.samples">

    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".QRcode"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SimpleTable"
            android:label="@string/simple_adapter"></activity>
        <activity
            android:name=".StyleTable"
            android:label="@string/style_adapter"></activity>
        <activity
            android:name=".FamilyTable"
            android:label="@string/family_adapter"></activity>
        <meta-data
            android:name="com.google.gms.vision.DEPENDENCIES"
            android:value="barcode" />
    </application>

</manifest>